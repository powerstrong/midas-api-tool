# MIDAS API Tool TODO

## 제품 방향
- 이 제품은 JSON 편집기가 아니라 MIDAS 입력 도구로 만든다.
- 사용자가 JSON을 직접 작성하거나 수정하게 하지 않는다.
- JSON payload 생성은 프로그램 내부에서 자동으로 처리한다.
- 핵심 가치는 MIDAS API를 비개발자도 엑셀처럼 다룰 수 있게 만드는 것이다.
- 사용자는 엑셀 데이터 복사/붙여넣기와 표 편집만으로 MIDAS DB를 입력할 수 있어야 한다.
- 각 기능은 MIDAS API 매뉴얼과 MIDAS 매뉴얼을 함께 참고해, 실무 맥락까지 설명하는 화면으로 만든다.

## 현재 우선순위
- [x] 하단 `알림`, `Payload 미리보기`, `검증 결과`를 하나의 `작업 결과` 패널로 통합
- [x] `Payload 미리보기` 기능 제거
- [x] 엔드포인트별 페이지 구조로 분리
- [x] 공통 테이블 셸과 엔드포인트별 정의/설명/검증 로직 분리
- [ ] 엑셀형 복붙 UX 강화

## 엑셀 UX 목표
- [x] 여러 `n x m` 블록 복붙 시 필요한 행을 자동 추가하면서 값 반영
- [x] 드래그 시 엑셀처럼 범위 선택
- [x] 직접 입력 시 현재 셀만 편집
- [x] 선택 영역 `Delete`/`Backspace`로 초기화
- [ ] 붙여넣기 기준 셀을 중심으로 다중 셀 반영

## 엔드포인트 구조 방향
- [x] `FBLA`, `STLD`, `CNLD`를 각자 페이지/모듈로 분리
- [x] 엔드포인트별 컬럼 정의 파일 분리
- [x] 엔드포인트별 검증 규칙 파일 분리
- [x] 엔드포인트별 도움말/설명 문구 파일 분리
- [x] 공통 액션바와 공통 결과 패널 재사용

## 설명 품질 방향
- [ ] API 매뉴얼 설명만이 아니라 MIDAS 매뉴얼 기준의 실무 설명 추가
- [ ] 필드 의미, 입력 예시, 자주 쓰는 값, 주의사항을 각 화면에 제공
- [ ] 실무자가 API 문서를 읽지 않아도 입력할 수 있는 수준으로 설명 강화

## 이후 확장
- [ ] `view/` 계열 엔드포인트 연동
- [ ] 테이블 위 편의 기능 추가
- [ ] 예: 선택 절점 가져오기
- [ ] 예: 현재 모델 값 불러오기
- [ ] 예: 선택 객체 기준 자동 채우기

## 작업 방식
- 이 문서를 기준으로 한 항목씩 처리한다.
- 구조 정리 작업을 먼저 하고, 이후 엔드포인트를 하나씩 완성한다.
- 사용자가 제공하는 API/GEN 매뉴얼을 기준으로 각 기능을 순차 구현한다.